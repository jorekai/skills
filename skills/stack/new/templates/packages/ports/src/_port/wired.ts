// The wired adapter of the {{PORT}} port. `lay.py --wire` rewrites the line below to the adapter
// the two axes chose; until then the offline adapter stands in, so the gate is green first.
export { adapter as wired } from "./memory.adapter";
