// The home page the generator wrote runs past the function bar, so the root replaces it with
// one that stays under it. Everything here is the project's to change.
const name = "{{NAME}}";

export default function Home() {
  return (
    <main>
      <h1>{name}</h1>
      <p>
        The gate is green before any account exists. Fill the keys in the secret store to wire a
        port.
      </p>
    </main>
  );
}
