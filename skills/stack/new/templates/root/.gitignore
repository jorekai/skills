node_modules/
.next/
dist/
.tsout/
coverage/
.env
.env.*
!.env.example
test-results/
playwright-report/
# What the gate leaves behind is not committed; the manifest of generated files is, because the
# drift guard reads it in the workflow.
.stack/*
!.stack/generated.json
*.tsbuildinfo
